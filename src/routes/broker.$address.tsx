import { createFileRoute, Link } from '@tanstack/react-router';
import { ArrowRight, BadgeCheck, LockKeyhole } from 'lucide-react';
export const Route=createFileRoute('/broker/$address')({component:PublicBroker});
function PublicBroker(){const {address}=Route.useParams();if(address.toLowerCase()!=='apexanchor')return <div className="max-w-xl mx-auto px-5 py-20 text-center"><h1 className="font-editorial text-4xl">Broker address not found</h1><Link to="/" className="btn-outline mt-6 inline-flex">Back to homepage</Link></div>;return <div className="max-w-4xl mx-auto px-5 py-16"><div className="card-warm p-8 md:p-12"><div className="flex items-center gap-2 text-primary text-xs uppercase tracking-[.2em]"><BadgeCheck size={16}/>Verified broker channel</div><h1 className="font-editorial text-5xl md:text-6xl mt-4">Work with ApexAnchor</h1><p className="text-lg text-muted-foreground mt-4">@apexanchor</p><p className="max-w-2xl mt-6 leading-relaxed">All offers, questions, and selling requests are handled privately through the single ApexAnchor broker. Buyers start a private thread from a listing. Sellers start one automatically when they submit an asset.</p><div className="mt-8 flex flex-wrap gap-3"><Link to="/browse" search={{kind:'sale'}} className="btn-primary">Buy an asset <ArrowRight size={16}/></Link><Link to="/sell" className="btn-outline">Sell an asset</Link><Link to="/auth" className="btn-ghost">Sign in</Link></div><div className="mt-10 border-t border-border pt-6 flex gap-3 text-sm text-muted-foreground"><LockKeyhole size={18} className="shrink-0"/><span>No seller phone numbers or email addresses are displayed publicly. Communication stays in private broker threads.</span></div></div></div>}
