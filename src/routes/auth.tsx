import { createFileRoute, useNavigate, useRouter } from '@tanstack/react-router';
import { useEffect, useState } from 'react';
import { supabase } from '@/integrations/supabase/client';
import { z } from 'zod';
export const Route=createFileRoute('/auth')({component:AuthPage});
const schema=z.object({email:z.string().trim().email('Enter a valid email').max(255),password:z.string().min(8,'At least 8 characters').max(72)});
function AuthPage(){const navigate=useNavigate();const router=useRouter();const [mode,setMode]=useState<'signin'|'signup'>('signin'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[name,setName]=useState(''),[loading,setLoading]=useState(false),[error,setError]=useState<string|null>(null);useEffect(()=>{supabase.auth.getSession().then(({data})=>{if(data.session)navigate({to:'/dashboard',replace:true})})},[navigate]);async function submit(e:React.FormEvent){e.preventDefault();setError(null);const p=schema.safeParse({email,password});if(!p.success)return setError(p.error.issues[0].message);if(mode==='signup'&&!name.trim())return setError('Enter a display name.');setLoading(true);try{const result=mode==='signup'?await supabase.auth.signUp({email:p.data.email,password:p.data.password,options:{emailRedirectTo:`${window.location.origin}${import.meta.env.BASE_URL}dashboard`,data:{display_name:name.trim()}}}):await supabase.auth.signInWithPassword(p.data);if(result.error)throw result.error;if(mode==='signup'&&!result.data.session){setError('Account created. Check your email to confirm your account.');return}router.invalidate();navigate({to:'/dashboard',replace:true})}catch(e){setError(e instanceof Error?e.message:'Something went wrong.')}finally{setLoading(false)}}async function google(){setError(null);const {error}=await supabase.auth.signInWithOAuth({provider:'google',options:{redirectTo:`${window.location.origin}${import.meta.env.BASE_URL}dashboard`}});if(error)setError('Google sign-in is not enabled for this Supabase project yet.');}
return <div className="min-h-[80vh] flex items-center justify-center px-5 py-16"><div className="w-full max-w-md"><p className="eyebrow text-center">ApexAnchor</p><h1 className="font-editorial text-4xl text-center mt-2">{mode==='signin'?'Welcome back':'Create your account'}</h1><p className="text-center text-muted-foreground mt-2">{mode==='signin'?'Sign in to list and message.':'A few details to get you started.'}</p><div className="card-warm p-6 mt-8"><button onClick={google} className="btn-outline w-full">Continue with Google</button><div className="flex items-center gap-3 my-5"><div className="rule flex-1"/><span className="text-xs text-muted-foreground">or</span><div className="rule flex-1"/></div><form onSubmit={submit} className="space-y-3">{mode==='signup'&&<div><label className="field-label">Display name</label><input className="input-field mt-1" value={name} onChange={e=>setName(e.target.value)} maxLength={80} required/></div>}<div><label className="field-label">Email</label><input type="email" required className="input-field mt-1" value={email} onChange={e=>setEmail(e.target.value)}/></div><div><label className="field-label">Password</label><input type="password" required className="input-field mt-1" value={password} onChange={e=>setPassword(e.target.value)} minLength={8}/></div>{error&&<p className="text-sm text-destructive">{error}</p>}<button disabled={loading} className="btn-primary w-full">{loading?'Please wait…':mode==='signin'?'Sign in':'Create account'}</button></form><p className="text-sm text-center mt-5 text-muted-foreground">{mode==='signin'?'New here?':'Already have an account?'} <button className="underline text-foreground" onClick={()=>setMode(mode==='signin'?'signup':'signin')}>{mode==='signin'?'Create an account':'Sign in'}</button></p></div></div></div>}
